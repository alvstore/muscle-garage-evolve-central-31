
import React from "react";
import { RouteObject } from "react-router-dom";
import { websiteRoutes } from "./websiteRoutes";

// Re-export the website routes as public routes
export const publicRoutes = websiteRoutes;
